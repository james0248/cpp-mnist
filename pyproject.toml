[project]
name = "cpp-mnist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.0.0",
    "numpy>=2.3.2",
    "pillow>=11.3.0",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.8",
]
